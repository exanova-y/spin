{
  "dependencies": {
    "marked": "^16.4.1"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "serve": "^14.2.5",
    "wrangler": "^4.99.0"
  },
  "scripts": {
    "start": "eleventy --serve",
    "build": "eleventy",
    "deploy": "eleventy && wrangler pages deploy _site"
  }
}
