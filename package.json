{
  "dependencies": {
    "marked": "^16.4.1",
    "vercel": "^54.14.5"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "serve": "^14.2.5",
    "wrangler": "^4.99.0"
  },
  "scripts": {
    "start": "eleventy --serve",
    "build": "rm -rf _site && eleventy",
    "deploy": "eleventy && wrangler pages deploy _site",
    "fetch:curius": "node scripts/fetch-curius.js",
    "embed:curius": "uv run scripts/embed-curius.py"
  }
}
